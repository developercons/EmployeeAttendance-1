<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="BtnGreen" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/btnColorGreen2</item>
        <item name="colorButtonNormal">@color/btnColorGreen</item>
    </style>

    <style name="BtnRed" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/btnColorRed2</item>
        <item name="colorButtonNormal">@color/btnColorRed</item>
    </style>

    <style name="BtnYellow" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/btnColorYellow2</item>
        <item name="colorButtonNormal">@color/btnColorYellow</item>
    </style>


</resources>
